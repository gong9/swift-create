export enum ProjectEnum {
  Business,
  Lib,
  Cli,
}

export enum FrameEnum {
  React,
  Vue,
}

export enum StateTask {
  Pending,
  Loading,
  Success,
  Error,
}

export enum ConfirmEnum {
  cancel,
  confirm,
}
